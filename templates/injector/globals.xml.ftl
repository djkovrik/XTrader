<globals>
    <#include "root://activities/common/common_globals.xml.ftl" />

    <global id="applicationClass" value="${applicationClass}" />
    <global id="applicationInterface" value="${applicationInterface}" />
    <global id="applicationProviderInterface" value="${applicationProviderInterface}" />
    <global id="corePackageName" value="${corePackageName}" />
</globals>
