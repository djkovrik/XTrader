<?xml version="1.0"?>
<template
    name="Core module"
    description="Creates a new core module."
    format="5"
    revision="5">

    <category value="Modular" />

    <formfactor value="Mobile" />

    <thumbs>
        <thumb>template_new_project.png</thumb>
    </thumbs>

    <parameter
        name="Core module name"
        constraints="nonempty|module|unique"
        default="core"
        id="moduleName"
        type="string" />

    <parameter
        name="Main Application interface"
        constraints="class|nonempty"
        default="App"
        help="The name of the application interface to create."
        id="applicationInterface"
        suggest="App"
        type="string" />

    <parameter
        name="Application provider interface"
        constraints="class|nonempty"
        default="ApplicationProvider"
        help="The name of the application provider interface to create."
        id="applicationProviderInterface"
        suggest="ApplicationProvider"
        type="string" />

    <parameter
        name="Package name"
        constraints="nonempty|package"
        default="com.sedsoftware.core"
        help="Path to core package module."
        id="corePackageName"
        suggest="com.sedsoftware.core"
        type="string" />

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
