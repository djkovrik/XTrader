detekt {
    version = Versions.DETEKT_VERSION

    profile("main") {
        config = "$rootDir/config/static-analysis/detekt-config.yml"
        filters = ".*test.*,.*/resources/.*,.*/tmp/.*"
        output = "$projectDir/reports/detekt.xml"
        baseline = "$projectDir/reports/baseline.xml"
    }
}